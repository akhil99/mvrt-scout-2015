<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppBase">

        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/accent</item>

        <item name="android:navigationBarColor">@color/primary</item>

        <item name="android:buttonStyle">@android:style/Widget.Material.Light.Button.Small</item>

    </style>

    <style name="mvrt_button" parent="android:Widget.Material.Button.Small">
        <item name="android:background">@drawable/mv_button</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@color/text_primary_light</item>
    </style>

</resources>
